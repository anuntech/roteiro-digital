services:
  roteiro-digital-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: roteiro-digital-backend
    restart: always
    ports:
      - "3333:3333"
    environment:
      - NODE_ENV=production
